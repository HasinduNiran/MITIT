# backend/.env.sample
# Copy to .env and fill values

NODE_ENV=development
PORT=5000

# Allow requests from your frontend dev server
FRONTEND_ORIGIN=http://localhost:5173

# Mongo connection string (example for local MongoDB)
MONGO_URI=mongodb+srv://lasalflowiix_db_user:FOMDbEvkF25z7R19@fastxprodbackup0129.hldrh6n.mongodb.net/MTIT_GPT?retryWrites=true&w=majority

# JWT signing secret (use a long random string)
JWT_SECRET=change_me_to_a_long_random_secret
